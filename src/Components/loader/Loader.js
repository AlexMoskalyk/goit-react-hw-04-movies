import React from "react";

import LoaderDots from "react-loader-spinner";

const Loader = () => {
  return <LoaderDots type="ThreeDots" color="#00BFFF" height={80} width={80} />;
};

export default Loader;
